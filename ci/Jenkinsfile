pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "gradle clean compile"
            }
        }
        stage('Test') {
            steps {
                sh "gradle test"
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
